# Nome da aplica��o
spring.application.name=Lista_carros

# Configura��o do DataSource
spring.datasource.url=jdbc:mysql://localhost:3306/ws_work_carros?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root

# Configura��o do Hibernate
spring.jpa.hibernate.ddl-auto=update

# Configura��o da JPA
spring.jpa.show-sql=true

# Tratamento de exce��es no Spring MVC
spring.mvc.throw-exception-if-no-handler-found=true
spring.mvc.dispatch-options-request=true

# Configura��es do servidor
#server.error.include-stacktrace=never
